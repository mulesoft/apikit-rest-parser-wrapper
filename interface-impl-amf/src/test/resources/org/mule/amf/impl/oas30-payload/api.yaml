openapi: "3.0.0"
info:
  version: 1.0.1
  title: example-api-testing
paths:
  /report:
    description: "example endpoint"
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: "./schemas/common.yaml#/components/schemas/report-request"
